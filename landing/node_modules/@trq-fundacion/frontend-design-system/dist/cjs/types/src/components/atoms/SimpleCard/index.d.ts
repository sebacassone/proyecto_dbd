import React from 'react';
import { SimpleCardProps as SimpleCardDefaultProps } from './Default';
export interface SimpleCardProps extends SimpleCardDefaultProps {
    type?: string;
}
/**
 * A Modal component that can render different types of modals.
 *
 * @component
 * @example
 * <Modal
 *   title="Modal Title"
 *   body="Modal content goes here."
 *   footer="Modal footer goes here."
 *   isOpen={true}
 *   onClose={() => console.log("Close modal")}
 * />
 */
declare const SimpleCard: React.FC<SimpleCardProps>;
export default SimpleCard;
